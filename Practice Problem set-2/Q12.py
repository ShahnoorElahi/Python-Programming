# 12. Write a program to calculate the length of string provide input
#     by user (without using len).


w1=str(input('Enter a string: '))

wCount=0

for i in w1:
    wCount+=1
print(wCount)
    
